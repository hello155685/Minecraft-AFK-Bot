name: Keep Aternos Awake
on:
  schedule:
    - cron: '*/5 * * * *' # Automatically runs every 5 minutes 24/7

jobs:
  ping-bot:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install Dependencies
        run: yarn install

      - name: Run Bot Login
        run: node bot.js
        env:
          CI: true
